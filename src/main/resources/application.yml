spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
        format_sql: false
        generate_statistics: false
        enable_lazy_load_no_trans: true
    show-sql: false
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false

springdoc:
  api-docs:
    path: /api-docs
  pathsToMatch: /**
  swagger-ui:
    path: /swagger-ui.html
grpc:
  port: 9090

graphql:
  servlet:
    mapping: /query
    enabled: true
    maxQueryDepth: 5
#    cors:
#      allowed-origins: "*"
#      allowed-headers: "Access-Control-Allow-Origin, access-control-allow-origin, access-control-allow-headers, Origin, X-Requested-With, Content-Type, Accept, Authorization"
#      allowed-methods: "GET,HEAD,OPTIONS,POST,PUT"
#    corsEnabled: false
graphql.playground:
  mapping: /playground
  endpoint: /graphql

