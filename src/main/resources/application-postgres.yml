spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: "jdbc:postgresql://localhost:5432/passnet_recruitment"
    username: passnet-recruitment-app
    password: tungduong98
    driverClassName: org.postgresql.Driver
  jpa:
    generate-ddl: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
        generate_statistics: false
        enable_lazy_load_no_trans: true
    show-sql: false
  h2:
    console:
      enabled: false
  redis:
    host: 192.168.99.100
    port: 6379
springdoc:
  api-docs:
    path: /api-docs
  pathsToMatch: /**
  swagger-ui:
    path: /swagger-ui.html
grpc:
  port: 9090

view:
  posted-jobs:
    id: postedJobViews

